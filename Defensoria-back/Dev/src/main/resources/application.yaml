spring:
  application:
    name: defensoria
  
  datasource:
    # 'db-defensoria' es el nombre del servicio en el docker-compose
    url: jdbc:postgresql://db-defensoria:5432/defensoria_db
    username: defensoria
    password: "Bicho_IPN_2026!"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true