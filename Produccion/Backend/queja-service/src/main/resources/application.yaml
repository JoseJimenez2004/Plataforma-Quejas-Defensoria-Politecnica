server:
  port: 8084

spring:
  application:
    name: queja-service
  datasource:
    url: jdbc:postgresql://localhost:5432/defensoria_db
    username: postgres
    password: Temporal2026@
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: defensoria-ddp-jwt-secret-produccion-muy-segura-2026

# Ruta local de pruebas para las evidencias de las quejas (cambia según tu OS)
storage:
  location: ./uploads
