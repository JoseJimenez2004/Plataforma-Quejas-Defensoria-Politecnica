package ipn.escom.defensoria.revision_service.entity;

/** Copia del enum de admin-service (mismo nombre de columna/valores en "personal_administrativo"
 * -- ambos servicios deben mantenerse en sincronía si se agrega un rol nuevo). */
public enum RolStaff {
    ADMIN_SISTEMAS,
    RECEPCIONISTA,
    ANALISTA_PRIMER_CONTACTO,
    SUBDEFENSOR,
    DEFENSOR
}
