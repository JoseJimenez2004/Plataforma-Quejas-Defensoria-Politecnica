server:
  port: 8089

spring:
  application:
    name: chatbot-service
  datasource:
    url: jdbc:postgresql://2.25.78.22:5432/defensoria_db
    username: postgres
    password: Temporal2026@
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

  cloud:
    compatibility-verifier:
      enabled: false

# Mismo secreto que el resto de microservicios -- este servicio nunca emite tokens, solo
# verifica los que ya emitió admin-service para proteger el CRUD de /api/chatbot/admin.
jwt:
  secret: defensoria-ddp-jwt-secret-produccion-muy-segura-2026
  expiration: 3600000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  pattern:
    level: "%5p [${spring.application.name}]"
