server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://2.25.78.22:5432/defensoria_db
    username: postgres
    password: Temporal2026@
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: defensoria-ddp-jwt-secret-produccion-muy-segura-2026

# Ruta externa en tu VPS para almacenar los PDFs de las evidencias de las quejas
storage:
  location: /apps/aplicaciones/defensoria/back/uploads
