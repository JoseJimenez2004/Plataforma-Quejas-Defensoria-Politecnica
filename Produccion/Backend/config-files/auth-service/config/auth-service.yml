server:
  # Puerto interno del contenedor unificado a 8080 (igual en los 9 microservicios) -- el
  # puerto real por el que se accede a este servicio sigue siendo 8083, solo que ahora es
  # el mapeo externo de Podman (ver -p en podman-compose.sh), no el puerto en el que escucha
  # Spring Boot dentro del contenedor. Mismo patrón que se usa en el trabajo con template_gio.
  port: 8080

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://2.25.78.22:5432/defensoria_db
    username: postgres
    password: Temporal2026@
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: jair100flo@gmail.com
    password: kstwxawsnsqkzhnk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true


  cloud:
    compatibility-verifier:
      enabled: false

jwt:
  secret: defensoria-ddp-jwt-secret-produccion-muy-segura-2026
  expiration: 3600000

quejas:
  service:
    url: http://2.25.78.22:8084

notificaciones:
  service:
    url: http://2.25.78.22:8085

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  pattern:
    level: "%5p [${spring.application.name}]"
